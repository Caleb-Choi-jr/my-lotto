import streamlit as st
import random
import time

# 1. 제목 꾸미기
st.title("💰 인생 역전! 로또 생성기")
st.subheader("이번 주 주인공은 바로 당신! 😎")

# 2. 버튼 클릭 시 동작
if st.button("🍀 5천 원어치(5줄) 번호 뽑기! 🍀"):
    
    # "두근두근" 효과 (스피너)
    with st.spinner('신중하게 번호를 고르고 있습니다...'):
        time.sleep(1.5) # 1.5초 뜸 들이기
    
    # 3. [핵심] 풍선 팡팡 효과! 🎉
    st.balloons() 
    # (겨울 느낌 내고 싶으면 st.snow() 로 바꿔보세요!)

    st.success("짜잔! 행운의 번호가 나왔습니다!")
    
    # 4. 5번 반복해서 번호 뽑기
    for i in range(1, 6):
        lotto_nums = random.sample(range(1, 46), 6)
        lotto_nums.sort()
        
        # 결과 보여주기
        st.write(f"**{i}번째 게임:** Running Man... 아니 로또맨! 🏃")
        st.info(f"{lotto_nums}")

# 5. 사이드바 (광고판)
st.sidebar.title("오늘의 운세")
st.sidebar.write("당첨되면 저한테 맛있는 거 사주세요! 🍔")
st.sidebar.button("개발자에게 커피 사주기 (마음만)")